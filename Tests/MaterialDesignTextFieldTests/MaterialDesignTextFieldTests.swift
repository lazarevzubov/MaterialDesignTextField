import XCTest
@testable import MaterialDesignTextField

final class MaterialDesignTextFieldTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MaterialDesignTextField().text, "Hello, World!")
    }
}
